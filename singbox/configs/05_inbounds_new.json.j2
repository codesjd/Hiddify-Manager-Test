{
    {# vless/vmess/trojan over ws/grpc/tcp/httpupgrade/xhttp used to render
       here whenever core_type=="singbox" - Xray now always serves these
       regardless of core_type (see xray/configs/05_inbounds_new.json.j2),
       so an xray-only client can use them no matter which core is primary.
       Sing-box no longer needs its own competing copy - keeping both would
       either fight over the same abns@v10-{protocol}-{stream}/127.0.0.1:50xx
       backend that haproxy/backends/v10.cfg.pj2 now always points at Xray
       for, or sit unused. #}
    "inbounds":[]
}
